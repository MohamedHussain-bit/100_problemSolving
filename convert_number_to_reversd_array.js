// change the number to string
// split the string to array
// change the string in array in number
// reverse to array
 function digitin(n){
    return n.toString()
    .splite("")
    .map((m) => Number(m))
    .reverse()
 }

 console.log(digitin(12345))
