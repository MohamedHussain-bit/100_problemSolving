// are you plaing banjo

function blaingBanjo(name){
    if(name.charAt(0) === "R" || "r"){
        return `${name} plaing banjo`
    } else {
        return `${name} no plaing banjo`
    }

}

console.log("Ream")
